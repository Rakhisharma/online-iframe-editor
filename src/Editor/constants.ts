const ERROR_TITLE = 'Error';

const ERROR_MESSAGE = 'Incorrect iframe format. Please try again ';

const CONTAINS_IFRAME_REGEX = /<iframe(?: [^>]*?)?(?:\/>|>.*?<\/iframe>)/i;
export { ERROR_TITLE, ERROR_MESSAGE, CONTAINS_IFRAME_REGEX };
